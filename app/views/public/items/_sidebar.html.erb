<div class="col-md-2">
  <table class="table table-brdered">
    <thead class="bg-light">
      <tr>
        <th>ジャンル検索</th>
      </tr>
    </thead>
    <% genres.each do |genre| %>
    <tr>
      <td><%= link_to genre.name, search_genre_path(genre) %></td>
    </tr>
  <% end %>
  </table>
</div>